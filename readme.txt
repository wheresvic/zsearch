Create a new local clone of your repository 

	git clone git@github.com:victorparmar/zsearch.git zsearch-html

Go into the new projectname-pages directory and checkout the gh-pages branch
	
	cd zsearch-html
	git checkout origin/gh-pages -b gh-pages

Start working on the gh-pages branch and push to origin gh-pages

	git add ...
	git commit -m ...
	git push origin gh-pages

